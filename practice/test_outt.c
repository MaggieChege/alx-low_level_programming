#include <stdio.h>

/**
 * main - Entry Point
 * Descriptions - prints alphabet in lowercase
 * Return: Returns 0 if code executes successfully
 */

int some_function_main(void)
{
    int i;

    for (i = 0; i < 10; i++)
    {
        printf("%d", i);
    }
    return(i);
}
