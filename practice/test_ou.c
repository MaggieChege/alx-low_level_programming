#include <stdio.h>

/**
 * main - Entry Point
 * Descriptions - prints alphabet in lowercase
 * Return: Returns 0 if code executes successfully
 */

int main(void)
{
    int i;

    i = 9;
    while (--i)
    {
        printf("%d", i);
    }

    return (0);
}
